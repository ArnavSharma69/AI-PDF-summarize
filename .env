# Path where ChromaDB will persist (created automatically)
CHROMA_PATH=./vectorstore

# SentenceTransformers model id
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Optional: use OpenAI for answer generation. If not set, the app returns context snippets.
OPENAI_API_KEY=
